/**
  기본 함수 형태 컴포넌트를 만들어야 된다.
  기본적으로 스니펫을 사용하거나 라이브러리를 추가하면
   `rfc` 라는 단축키를 이용해서 아래와 같이 기본 컴포넌트를 만들 수 있다!
*/
import React from 'react';

export default function Header() {
  return <div>Header</div>;
}
